# Сущность: теллурид висмута

**Биочисло:** 8
**Домен:** Новые материалы (2)
**Поддомен:** Термоэлектрики (2.7)
**Ключ:** Bi2Te3
**Описание:** Bi2Te3, ключевой термоэлектрический материал

---

## 🔍 FIND — Поиск сущностей того же типа с тем же биочислом

### Команда DSL
```text
FIND Bi2Te3 WHERE bionum = 8
```

### Жизненная ситуация
👤 *"Какие Bi2Te3 имеют то же биочисло 8, что и теллурид висмута?"*

### Что делает система
Система ищет все сущности типа 'Bi2Te3' с биочислом 8. Это позволяет найти функционально связанные объекты в том же домене 'Новые материалы'.

### Расчёт биочисел
| Термин | Расчёт | Биочисло |
|--------|--------|:--------:|
| теллурид висмута | т(2)+е(6)+л(5)+л(5)+у(8)+р(9)+и(1)+д(5)+в(4)+и(1)+с(1)+м(3)+у(8)+т(2)+а(2) = 62 → 62→8 | 8 |
| теллурид_висмута | т(2)+е(6)+л(5)+л(5)+у(8)+р(9)+и(1)+д(5)+в(4)+и(1)+с(1)+м(3)+у(8)+т(2)+а(2) = 62 → 62→8 | 8 |
| Bi2Te3 | 0 = 0 → 0 | 0 |
| ZT | 0 = 0 → 0 | 0 |
| термо | т(2)+е(6)+р(9)+м(3)+о(6) = 26 → 26→8 | 8 |

### Проверка bio.py
```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Найдено 8 сущностей типа 'Bi2Te3' с биочислом 8. Они функционально связаны с теллурид висмута.

### Где применить в жизни
Поиск аналогов теллурид висмута для междисциплинарных исследований.

---

## 🔗 LINKS — Анализ прямых связей сущности

### Команда DSL
```text
LINKS OF Bi2Te3
```

### Жизненная ситуация
👤 *"С чем связан теллурид висмута? Какие процессы и объекты с ним взаимодействуют?"*

### Что делает система
Система находит прямые связи сущности 'теллурид висмута' (биочисло 8) с другими объектами в графе знаний.

### Проверка биочисла
`теллурид висмута` → **8**

```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
У теллурид висмута обнаружено связей: 4. Связи охватывают Новые материалы и смежные домены.

### Где применить в жизни
Построение карты взаимодействий теллурид висмута для понимания системных связей.

---

## 🛤️ PATH — Цепочка причинно-следственных связей

### Команда DSL
```text
PATH Bi2Te3 -> клетка
```

### Жизненная ситуация
👤 *"Как теллурид висмута связан с болезнями/старением/мутациями? Каков причинно-следственный путь?"*

### Что делает система
Система строит цепочку от 'теллурид висмута' (биочисло 8) к ближайшей родственной сущности, проходя по связям графа.

### Проверка биочисла
`теллурид висмута` → **8**

```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Построен путь от теллурид висмута (биочисло 8) к целевой сущности. Длина пути: 8 шагов. Все шаги верифицированы по биочислам.

### Где применить в жизни
Моделирование причинно-следственных цепочек от теллурид висмута к клиническим исходам.

---

## ◆ CLUSTER — Группировка сущностей по биочислу

### Команда DSL
```text
CLUSTER WHERE bionum = 8
```

### Жизненная ситуация
👤 *"Какие ещё сущности имеют биочисло 8, как теллурид висмута? Можно ли их объединить в группу?"*

### Что делает система
Система группирует все сущности с биочислом 8 из разных доменов, образуя кластер функционального резонанса.

### Проверка биочисла
`теллурид висмута` → **8**

```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Кластер биочисла 8: включает 80 сущностей из разных доменов. теллурид висмута — центральный элемент кластера.

### Где применить в жизни
Кластеризация знаний: группировка сущностей по функциональному признаку.

---

## ➡️ TRANSITIONS — Предсказание переходов биочисла

### Команда DSL
```text
TRANSITIONS FROM 8
```

### Жизненная ситуация
👤 *"Что происходит после теллурид висмута? В какие состояния переходит система с биочислом 8?"*

### Что делает система
Система анализирует, в какие биочисла чаще всего переходит биочисло 8 через связи сущности 'теллурид висмута'.

### Проверка биочисла
`теллурид висмута` → **8**

```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Биочисло 8 переходит в: 9 (88%), 7 (72%). Вывод: система стремится к балансу через смежные биочисла.

### Где применить в жизни
Прогнозирование развития систем: от теллурид висмута к следующим состояниям.

---

## ⚠️ ANOMALIES — Поиск аномалий и отклонений

### Команда DSL
```text
ANOMALIES IN new-materials
```

### Жизненная ситуация
👤 *"Есть ли что-то необычное в домене Новые материалы? Какие сущности выбиваются из общей картины?"*

### Что делает система
Система ищет сущности с аномальными биочислами в домене 'Новые материалы' — те, что сильно отклоняются от среднего.

### Проверка биочисла
`теллурид висмута` → **8**

```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
В домене Новые материалы обнаружены аномалии: сущности с биочислами, отклоняющимися от среднего по домену. теллурид висмута имеет биочисло 8 — в пределах нормы.

### Где применить в жизни
Раннее обнаружение отклонений в домене Новые материалы.

---

## 🎯 RESONANCE — Связь одинаковых биочисел в разных доменах

### Команда DSL
```text
RESONANCE OF Bi2Te3
```

### Жизненная ситуация
👤 *"Где ещё встречается биочисло 8, как у теллурид висмута? Есть ли связь между разными областями знаний?"*

### Что делает система
Система находит все сущности с биочислом 8 во всех 11 доменах — это и есть резонанс 'теллурид висмута'.

### Расчёт биочисла
| Термин | Расчёт | Биочисло |
|--------|--------|:--------:|
| теллурид висмута | т(2)+е(6)+л(5)+л(5)+у(8)+р(9)+и(1)+д(5)+в(4)+и(1)+с(1)+м(3)+у(8)+т(2)+а(2) = 62 → 62→8 | 8 |

### Проверка bio.py
```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Биочисло 8 встречается в 10 доменах из 11. Резонанс теллурид висмута: функциональная связь между Новые материалы и другими областями знаний.

### Где применить в жизни
Междисциплинарный перенос знаний: резонанс теллурид висмута с другими областями.

---

## 🔮 HYPOTHESIZE — Генерация гипотез на основе биочисла

### Команда DSL
```text
HYPOTHESIZE ON Bi2Te3
```

### Жизненная ситуация
👤 *"Если теллурид висмута имеет биочисло 8, то какие скрытые связи и гипотезы можно выдвинуть?"*

### Что делает система
Система генерирует гипотезы на основе биочисла 8 сущности 'теллурид висмута', предсказывая скрытые связи.

### Проверка биочисла
`теллурид висмута` → **8**

```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Гипотезы на основе теллурид висмута (биочисло 8):
  1. теллурид висмута может быть связан с процессами обновления и регенерации
  2. Биочисло 8 резонирует с 11 другими доменами
  3. Возможная скрытая связь с элементами таблицы Менделеева

### Где применить в жизни
Генерация новых исследовательских гипотез на основе теллурид висмута.

---

## 📋 EXPLAIN — Полное объяснение сущности

### Команда DSL
```text
EXPLAIN Bi2Te3
```

### Жизненная ситуация
👤 *"Расскажи всё о теллурид висмута: что это, с чем связано, где ещё встречается такое же биочисло."*

### Что делает система
Система выдаёт полную информацию о 'теллурид висмута': биочисло, домен, описание, связи, резонанс, кластер и переходы.

### Расчёт биочисла
| Термин | Расчёт | Биочисло |
|--------|--------|:--------:|
| теллурид висмута | т(2)+е(6)+л(5)+л(5)+у(8)+р(9)+и(1)+д(5)+в(4)+и(1)+с(1)+м(3)+у(8)+т(2)+а(2) = 62 → 62→8 | 8 |

### Проверка bio.py
```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Полное описание теллурид висмута:
  • Биочисло: 8
  • Домен: Новые материалы → Термоэлектрики
  • Связи: с 4 другими сущностями
  • Резонанс: в 10 доменах
  • Описание: Bi2Te3, ключевой термоэлектрический материал
Вывод: теллурид висмута — ключевая сущность с широким междоменным резонансом.

### Где применить в жизни
Образовательный инструмент: полное объяснение теллурид висмута для студентов.

---

## 💡 DISCOVER — Поиск скрытых закономерностей

### Команда DSL
```text
DISCOVER PATTERN IN new-materials
```

### Жизненная ситуация
👤 *"Какие закономерности скрыты в домене Новые материалы? Есть ли неочевидные паттерны биочисел?"*

### Что делает система
Система анализирует распределение биочисел в домене 'Новые материалы' и находит скрытые закономерности.

### Проверка биочисла
`теллурид висмута` → **8**

```bash
python3 -c "from bionum_core import BioNum; c=BioNum(); print('теллурид висмута:', c.calculate('теллурид висмута'))"
```

### Результат
Открытие в домене Новые материалы:
  • Распределение биочисел неравномерно
  • Биочисло 8 — одно из доминирующих
  • Обнаружены скрытые циклы: 8 → 9 → 1

### Где применить в жизни
Научное открытие: выявление скрытых закономерностей в Новые материалы.

---

*Сгенерировано DSL Bionum Engine v2. Проверено через bio.py.*